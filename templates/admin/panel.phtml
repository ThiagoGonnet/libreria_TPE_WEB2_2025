<?php include "templates/layout/header.phtml"; ?>

<h2>Panel de Administración</h2>
<p>Bienvenido, <?= htmlspecialchars($user); ?>!</p>
<a href="<?= BASE_URL ?>logout">Cerrar sesión</a>

<h3>Libros</h3>
<table>
    <thead>
        <tr>
            <th>Título</th>
            <th>Autor</th>
            <th>Fecha de Publicación</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (!empty($books)) : ?>
            <?php foreach ($books as $book) : ?>
                <tr>
                    <td><?= htmlspecialchars($book->titulo) ?></td>
                    <td><?= htmlspecialchars($book->autor) ?></td>
                    <td><?= $book->fecha_de_publicacion ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>panel/editBook/<?= $book->id ?>">Editar</a>
                        <a href="<?= BASE_URL ?>panel/deleteBook/<?= $book->id ?>">Eliminar</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php else: ?>
            <tr>
                <td colspan="4">No hay libros cargados</td>
            </tr>
        <?php endif; ?>
    </tbody>
</table>

<h3>Autores</h3>
<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Nacionalidad</th>
            <th>Fecha de Nacimiento</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (!empty($authors)) : ?>
            <?php foreach ($authors as $author) : ?>
                <tr>
                    <td><?= htmlspecialchars($author->nombre) ?></td>
                    <td><?= htmlspecialchars($author->nacionalidad) ?></td>
                    <td><?= $author->fecha_de_nacimiento ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>panel/editAuthor/<?= $author->id ?>">Editar</a>
                        <a href="<?= BASE_URL ?>panel/deleteAuthor/<?= $author->id ?>">Eliminar</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php else: ?>
            <tr>
                <td colspan="4">No hay autores cargados</td>
            </tr>
        <?php endif; ?>
    </tbody>
</table>
