<?php include "templates/layout/header.phtml"; ?>

<h2>Panel de Administración</h2>
<p>Bienvenido, <?= htmlspecialchars($user); ?>!</p>
<a href="<?= BASE_URL ?>logout">Cerrar sesión</a>

<!-- ==================== LIBROS ==================== -->
<h3>Libros</h3>
<table>
    <thead>
        <tr>
            <th>Título</th>
            <th>Autor</th>
            <th>Fecha de Publicación</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (!empty($books)) : ?>
            <?php foreach ($books as $book) : ?>
                <tr>
                    <td><?= htmlspecialchars($book->titulo) ?></td>
                    <td><?= htmlspecialchars($book->autor) ?></td>
                    <td><?= $book->fecha_de_publicacion ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>panel/editBook/<?= $book->id ?>">Editar</a> |
                        <a href="<?= BASE_URL ?>panel/deleteBook/<?= $book->id ?>">Eliminar</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php else: ?>
            <tr>
                <td colspan="5">No hay libros cargados</td>
            </tr>
        <?php endif; ?>
    </tbody>
</table>

<!-- ===== FORMULARIO AGREGAR LIBRO ===== -->
<h3>Agregar Libro</h3>
<form action="<?= BASE_URL ?>panel/addBook" method="POST">
    <label for="titulo">Título:</label>
    <input type="text" name="titulo" id="titulo" required>

    <label for="autor_id">Autor existente:</label>
    <select name="autor_id" id="autor_id">
        <option value="">-- Seleccionar autor --</option>
        <?php foreach ($authors as $author) : ?>
            <option value="<?= $author->id ?>"><?= htmlspecialchars($author->nombre) ?></option>
        <?php endforeach; ?>
    </select>

    <p>O autor nuevo:</p>
    <label for="nuevo_autor">Nombre del autor:</label>
    <input type="text" name="nuevo_autor" id="nuevo_autor">

    <label for="fecha_de_publicacion">Fecha de publicación:</label>
    <input type="date" name="fecha_de_publicacion" id="fecha_de_publicacion">

    <button type="submit">Agregar libro</button>
</form>

<!-- ==================== AUTORES ==================== -->
<h3>Autores</h3>
<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Nacionalidad</th>
            <th>Fecha de Nacimiento</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (!empty($authors)) : ?>
            <?php foreach ($authors as $author) : ?>
                <tr>
                    <td><?= htmlspecialchars($author->nombre) ?></td>
                    <td><?= htmlspecialchars($author->nacionalidad) ?></td>
                    <td><?= $author->fecha_de_nacimiento ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>panel/editAuthor/<?= $author->id ?>">Editar</a> |
                        <a href="<?= BASE_URL ?>panel/deleteAuthor/<?= $author->id ?>">Eliminar</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php else: ?>
            <tr>
                <td colspan="4">No hay autores cargados</td>
            </tr>
        <?php endif; ?>
    </tbody>
</table>

<!-- ===== FORMULARIO AGREGAR AUTOR ===== -->
<h3>Agregar Autor</h3>
<form action="<?= BASE_URL ?>panel/addAuthor" method="POST">
    <label for="nombre">Nombre:</label>
    <input type="text" name="nombre" id="nombre" required>

    <label for="nacionalidad">Nacionalidad:</label>
    <input type="text" name="nacionalidad" id="nacionalidad">

    <label for="fecha_de_nacimiento">Fecha de nacimiento:</label>
    <input type="date" name="fecha_de_nacimiento" id="fecha_de_nacimiento">

    <button type="submit">Agregar autor</button>
</form>
