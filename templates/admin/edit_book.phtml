<h2>Editar Libro</h2>
<form action="<?= BASE_URL ?>panel/editBook/<?= $book->id ?>" method="POST">
  <label>Título:</label>
  <input type="text" name="titulo" value="<?= htmlspecialchars($book->titulo) ?>" required>

  <label>Autor:</label>
  <select name="autor_id" required>
    <?php foreach ($authors as $author): ?>
      <option value="<?= $author->id ?>" <?= $author->id == $book->autor_id ? 'selected' : '' ?>>
        <?= htmlspecialchars($author->nombre) ?>
      </option>
    <?php endforeach; ?>
  </select>

  <label>Fecha de publicación:</label>
  <input type="date" name="fecha_de_publicacion" value="<?= $book->fecha_de_publicacion ?>" required>

  <button type="submit">Guardar cambios</button>
</form>
